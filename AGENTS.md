# AGENTS.md --- 车队经营管理系统开发总纲

## 1. 目标

本仓库实现一个由微信小程序、Java 后端、Vue3 Web
管理端组成的车队经营管理系统。 开发必须以 `/docs`
下的项目基线文档、`/database/database.sql` 和 `/docs/openapi.yaml`
为依据，不得凭经验擅自改变已确认业务规则。

## 2. 事实来源与优先级

发生冲突时按以下优先级处理： 1.
`01-项目需求规格说明书-V1.0`：业务规则最高优先级。 2.
`02-完整功能与数据库ER设计-V1.0`：模块、页面、实体关系。 3.
`03-MySQL物理表结构设计-V1.0`：数据库物理结构。 4.
`04-API接口设计说明书-V1.0` 与 `openapi.yaml`：接口契约。 5.
`05-页面交互与状态设计说明书-V1.0`：前端交互。 6.
`06-权限矩阵与错误码校验规则-V1.0`：权限、错误码、强校验。 7.
`07-测试与验收用例说明书-V1.0`：验收基线。

若文档之间出现无法按优先级消解的冲突，停止相关实现并明确报告冲突，不得自行猜测。

## 3. 核心业务不可违反

-   线路核心分配对象是司机，车辆跟随司机当前绑定关系。
-   发布、发车、重新启用都必须在后端执行司机/车辆强校验。
-   发车时冻结司机、车辆、车挂快照；运输中禁止修改司机-车辆和车辆-车挂绑定。
-   卸货完成后才允许正常换绑；运输中线路只能作废，不能取消。
-   毛重、皮重同页提交，必须毛重 \>
    皮重；实际载重由系统计算；不达标不得阻断卸货。
-   卸货后司机不能改重量；授权人员可纠错，原因必填、图片选填，且原始值和每次修改历史永久保留。
-   所有费用必须绑定车辆；费用分为线路费用和日常费用；日常费用绝不分摊到线路。
-   审批通过费用不可直接修改，错误必须冲销后重新发起。
-   电费/气费可按车辆及发车---卸货时间自动匹配线路，允许财务后续纠正。
-   审批流程维护期间禁止新发起；有在途审批时禁止修改流程。
-   D 打回 B 时后续重新执行 B→C→D；申请变化后后续旧审批结果不可复用。
-   所有导入必须预览、预校验、确认、正式再次校验、逐行结果、保存历史；重复数据必须失败并说明原因。
-   轮胎换胎保存原图、OCR
    原始结果、用户确认胎号；已申领轮胎不可再次申请。
-   实时利润始终按当前有效数据计算；月度结算版本不可变；新版本必须产生可解释的差异明细。
-   一个用户只能有一个角色；外协司机只能执行核心运输流程。

## 4. 工程原则

-   前端隐藏/禁用按钮不是安全边界；所有权限和状态必须由后端再次校验。
-   发布、发车、卸货、作废、重新启用、审批、冲销、换胎消耗、正式导入等使用事务。
-   关键动作实现幂等；数据库唯一约束作为最终防重。
-   不使用浮点类型存储金额和重量；使用 DECIMAL。
-   业务删除优先逻辑删除；审批、结算、运输、审计历史不得物理破坏。
-   多态附件、审计、多态业务关联不强制建立数据库外键；复杂一致性由服务层保证。
-   密码不做复杂度要求，但必须安全哈希存储，禁止明文。
-   不提前实现 V1.0 未要求的组织/部门数据隔离、旧胎精细生命周期等功能。

## 5. 推荐开发顺序

0.  项目骨架、数据库迁移、统一响应、异常、日志、鉴权。
1.  用户、角色、权限、登录。
2.  司机、车辆、车挂、绑定历史、运输锁。
3.  客户、产品、线路、发布、发车、重量、卸货、作废、重新启用。
4.  统一导入中心。
5.  审批引擎。
6.  费用、冲销、电费/气费匹配。
7.  轮胎、OCR、换胎。
8.  利润、报表、看板。
9.  月度结算与版本差异。
10. 微信小程序。
11. Vue3 管理端。
12. 全量测试与验收。

## 6. 每个阶段的工作方式

开发阶段开始前先读取对应文档和 OpenAPI/SQL。 实现后必须： -
运行该模块单元测试/集成测试。 - 对照 `07-测试与验收用例说明书-V1.0`。 -
检查权限、状态机、审计、幂等和并发。 - 不通过验收不得进入下一阶段。

## 7. 目录建议

-   `/docs`：设计与接口契约。
-   `/database`：建库/迁移脚本。
-   `/backend`：Java 后端。
-   `/admin-web`：Vue3 管理端。
-   `/mini-program`：微信小程序。

## 8. 变更纪律

任何改变业务状态机、利润口径、费用归属、审批规则、唯一性规则或数据库核心关系的修改，都视为需求变更。
先更新对应设计文档和测试基线，再修改代码。
